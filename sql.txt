CREATE TABLE livros (
	id_livro         integer GENERATED BY DEFAULT AS IDENTITY,
	titulo           varchar(255) NOT NULL,
	autor varchar(255) NOT NULL,
	ano_publicacao   integer
);

ALTER TABLE livros ADD PRIMARY KEY (id_livro);

INSERT INTO LIVROS(titulo, autor, ano_publicacao)
values('A Culpa é das estrelas', 'John Green', 2007)

INSERT INTO LIVROS(titulo, autor, ano_publicacao)
values('A Revolução dos Bichos', 'George Orwell', 2008)

INSERT INTO LIVROS(titulo, autor, ano_publicacao)
values('Os Miseráveis', 'Victor Hugo', 2009)


SELECT * FROM LIVROS l 



select id_livro, titulo, autor, ano_publicacao from livros WHERE id_livro = 14



update livros set titulo = ? , autor = ?, ano_publicacao where id_livro = 14", (titulo,autor,ano_publicacao, id))